# Red Team Methodology — 30 Advanced Attacker-Mindset Techniques
# منهجية الريد تيم — ٣٠ تقنية متقدمة بعقلية مهاجم

> **Authorized testing only / للاختبار المُصرّح به فقط.** استخدم هذه المنهجية على
> أصولك أو ضمن نطاق برنامج bug bounty معتمد. كل الخطوات هنا **تحقّق يدوي غير
> تدميري** باستخدام حسابات وموارد تملكها. لا تُنفّذ استغلالًا فعليًا على بيانات
> مستخدمين حقيقيين، ولا DoS، ولا أي شيء خارج النطاق.

كيف تربط الأداة بالمنهجية: `ayed_smart_api_hunter.py` يعطيك **سطح الهجوم مصنّفًا
بطبقات خطورة** + **أعلام (flags)**. كل تقنية أدناه تبدأ من *إشارة* تراها في تقرير
الأداة، ثم *عقلية المهاجم*، ثم *خطوات التحقق*، ثم *الأثر*.

---

## عقلية المهاجم — سلسلة القتل (Kill Chain)
1. **Recon** — اجمع كل سطح الهجوم (subdomains, JS, params, APIs).
2. **Map** — افهم الأدوار، تدفّق الحالة، المعرّفات (IDs)، والثقة بين المكوّنات.
3. **Identify trust boundaries** — أين ينتقل الطلب من "مجهول" إلى "موثوق"؟
4. **Break assumptions** — كل افتراض ضمني (client يتحقق، ID عشوائي، الترتيب ثابت)
   هو ثغرة محتملة.
5. **Chain** — الثغرة الصغيرة × الثغرة الصغيرة = أثر حرج (منخفض + open redirect =
   استيلاء حساب).
6. **Prove impact** — لا تُبلّغ عن "احتمال"، بل عن أثر ملموس على مورد تملكه.

---

## أولًا: التحكم بالوصول (Access Control / Authorization)

### 1. IDOR / BOLA — انعدام تفويض على مستوى الكائن
- **إشارة الأداة:** `tier=HIGH/CRITICAL`, tag `identity`/`api`, بارامترات مثل
  `id/uid/order_id/account_id`, علم `JSON_API`.
- **العقلية:** الخادم يثق أن `id=5` يعود لي لأني مسجّل دخول — بدون التحقق من الملكية.
- **التحقق:** أنشئ حسابين تملكهما (A و B). نفّذ الطلب بحساب A على مورد يخص B
  (بدّل المعرّف). لاحظ إن رجعت بيانات B → BOLA. جرّب أيضًا معرّفات متسلسلة، UUID
  مسرّبة في ردود أخرى، وترميز (base64/hashids).
- **الأثر:** قراءة/تعديل بيانات مستخدمين آخرين.

### 2. BFLA — انعدام تفويض على مستوى الوظيفة
- **إشارة الأداة:** علم `STATE_METHODS_ADVERTISED` (PUT/PATCH/DELETE)، tag `privileged`.
- **العقلية:** نقطة "admin only" لكن التفويض يُفحص في الواجهة لا في الـAPI.
- **التحقق:** بحساب منخفض الصلاحية، استدعِ مسارات/أفعال إدارية (`/api/admin/...`،
  أو نفس المورد بـmethod مختلف). راقب هل يُنفّذ. **لا تستدعِ DELETE عشوائيًا** — استعمل
  موردًا أنشأته أنت.
- **الأثر:** رفع صلاحيات، تنفيذ وظائف إدارية.

### 3. Mass Assignment / Parameter Tampering
- **إشارة الأداة:** endpoints JSON تقبل POST/PUT مع نماذج (forms) أو schema.
- **العقلية:** الـobject binding يقبل حقولًا لم تُعرض في الواجهة (`role`, `is_admin`,
  `verified`, `balance`).
- **التحقق:** أضف حقولًا حساسة إلى جسم الطلب على حسابك، ثم اقرأ ملفك للتحقق هل تغيّرت
  القيمة فعلًا (مثلًا `"role":"admin"`).
- **الأثر:** رفع صلاحيات، تعديل حقول محمية.

### 4. Broken Workflow / State Machine Bypass
- **العقلية:** التطبيق يفترض تنفيذ الخطوات بالترتيب (سلة → دفع → تأكيد). ماذا لو قفزت؟
- **التحقق:** أعد ترتيب/تخطّي الخطوات (اطلب خطوة التأكيد قبل الدفع، أعد استخدام
  توكن خطوة سابقة، كرّر خطوة نهائية). راقب هل يُقبل.
- **الأثر:** شراء بلا دفع، تجاوز تحقّق، الوصول لموارد غير مكتملة.

### 5. 401/403 Bypass
- **إشارة الأداة:** علم `FORBIDDEN`/`AUTHENTICATION_REQUIRED`.
- **العقلية:** الحجب يعتمد على مطابقة سطحية للمسار/الأسلوب.
- **التحقق (توثيقيًا):** جرّب `/admin` vs `/admin/`، `/Admin`, `/admin/.`,
  `%2e`, path traversal للـACL، هيدرات `X-Original-URL`/`X-Rewrite-URL`/
  `X-Forwarded-For`/`X-Forwarded-Host`, تبديل GET↔POST، إضافة امتداد `.json`.
- **الأثر:** الوصول لمحتوى محجوب.

---

## ثانيًا: المصادقة والجلسات (Auth & Session)

### 6. JWT Attacks
- **إشارة الأداة:** توكنات في الردود/الكوكيز، مسارات `auth`/`oauth`.
- **العقلية:** التحقق من التوقيع ضعيف أو قابل للخداع.
- **التحقق:** فكّ الـJWT (base64). اختبر `alg:none`، مفتاح سري ضعيف (قاموس على توكن
  تملكه)، `kid` injection/path traversal، خلط RS256→HS256، انتهاء صلاحية غير مُطبّق.
  استعمل توكن حسابك فقط.
- **الأثر:** انتحال هوية، تجاوز مصادقة.

### 7. OAuth / OIDC Flow Abuse
- **إشارة الأداة:** tags `auth`, params `redirect_uri`/`state`/`code`/`callback`.
- **العقلية:** الثقة في `redirect_uri` أو غياب `state`/PKCE.
- **التحقق:** جرّب `redirect_uri` لنطاق تتحكم به (أو subdomain/مسار مفتوح)،
  غياب/عدم التحقق من `state` (CSRF على الربط)، تسريب `code`/`token` عبر Referer أو
  open redirect، إعادة استخدام `code`.
- **الأثر:** استيلاء على حساب (Account Takeover).

### 8. Password Reset Flaws
- **العقلية:** رابط إعادة التعيين يُبنى من مدخلات يتحكم بها المهاجم.
- **التحقق:** Host Header Injection (`Host:`/`X-Forwarded-Host`) لتسميم رابط
  الاستعادة، توكنات قابلة للتنبؤ/قصيرة، عدم إبطال التوكن بعد الاستخدام، تسريب التوكن
  في الرد. اختبر على حسابك.
- **الأثر:** استيلاء على حساب.

### 9. Session Fixation / Cookie Scoping
- **إشارة الأداة:** علم `WEAK_COOKIE_FLAGS` (بدون Secure/HttpOnly/SameSite).
- **العقلية:** الجلسة لا تتجدّد بعد الدخول، أو الكوكي واسع النطاق (`.domain`).
- **التحقق:** هل يتغيّر معرّف الجلسة بعد login؟ هل الكوكي مشترك عبر subdomains غير موثوقة؟
- **الأثر:** سرقة جلسة، CSRF.

### 10. MFA / 2FA Bypass (منطقي)
- **العقلية:** خطوة الـ2FA منفصلة عن إصدار الجلسة.
- **التحقق:** هل الجلسة صالحة قبل إكمال 2FA؟ إعادة استخدام رمز، Brute بلا rate-limit
  (لاحظ فقط، لا تُنفّذ هجومًا فعليًا)، تخطّي الخطوة بطلب المورد النهائي مباشرة، تعديل
  `mfa_verified=true`.
- **الأثر:** تجاوز الطبقة الثانية.

---

## ثالثًا: الإدخال والحقن (Injection — رصد غير تدميري)

### 11. XSS Context Discovery
- **إشارة الأداة:** علم `REFLECTION` (علامة انعكست في الجسم).
- **العقلية:** أين ينعكس الإدخال (HTML/attribute/JS/URL) وهل الترميز مفقود؟
- **التحقق:** افحص سياق الانعكاس يدويًا؛ استعمل قيمًا حميدة (`ayedTEST<>"'`) لرؤية أي
  رموز تمر بلا ترميز. لا تُطلق حمولات فعلية على الإنتاج.
- **الأثر:** تنفيذ سكربت، سرقة جلسة.

### 12. SSRF عبر بارامترات URL/webhook
- **إشارة الأداة:** tags `callback`/`file`, params `url/uri/dest/webhook`.
- **العقلية:** الخادم يجلب URL يتحكم به المستخدم.
- **التحقق:** وجّه البارامتر إلى Collaborator/خادم تملكه وراقب الاتصال الوارد. اختبر
  `169.254.169.254` (metadata) و`localhost` **فقط ضمن نطاق مصرّح صراحة بذلك**.
- **الأثر:** الوصول للشبكة الداخلية/بيانات السحابة.

### 13. SSTI / CSTI
- **العقلية:** مدخلاتك تُدمج في قالب على الخادم/العميل.
- **التحقق:** أدخل تعبيرًا حسابيًا حميدًا (`{{7*7}}`، `${7*7}`) في حقول تنعكس؛ إن ظهر
  `49` فهناك تقييم قالب. توثيق فقط.
- **الأثر:** RCE (خادم) / XSS (عميل).

### 14. SQL / NoSQL Signal (رصد فقط)
- **العقلية:** الأخطاء أو فروق التوقيت تكشف حقن استعلام.
- **التحقق:** لاحظ رسائل خطأ قاعدة البيانات في الردود، فروق سلوك مع `'` أو `[$ne]`،
  دون تنفيذ استخراج بيانات. سجّل الإشارة وبلّغ.
- **الأثر:** تسريب/تعديل بيانات.

### 15. XXE
- **إشارة الأداة:** endpoints تقبل `application/xml` أو SOAP.
- **العقلية:** المحلّل يعالج كيانات خارجية.
- **التحقق:** أرسل XML بكيان يشير إلى خادمك (OOB) وراقب الاتصال؛ لا تقرأ ملفات حساسة
  خارج النطاق.
- **الأثر:** قراءة ملفات، SSRF.

### 16. Deserialization Surface
- **العقلية:** كائنات مُسلسلة (Java/PHP/.NET/pickle) تُفكّ من مدخلات المستخدم.
- **التحقق:** حدّد الكوكيز/الحقول التي تحمل بيانات مُسلسلة (بصمات مثل `rO0`, `O:8:`)؛
  وثّق السطح وبلّغ دون إطلاق gadget chains على الإنتاج.
- **الأثر:** RCE.

---

## رابعًا: طبقة HTTP

### 17. HTTP Request Smuggling
- **العقلية:** خلاف بين الـfront-end والـback-end في تفسير `Content-Length`/
  `Transfer-Encoding`.
- **التحقق:** اختبارات CL.TE/TE.CL بحذر شديد وضمن نطاق يسمح بها؛ استعمل تقنيات
  زمنية آمنة أولًا. **قد يؤثر على مستخدمين آخرين — تجنّبه على الإنتاج المشترك إلا
  بإذن صريح.**
- **الأثر:** تسميم طلبات، تجاوز أمن.

### 18. Web Cache Poisoning / Deception
- **العقلية:** إدخال غير مُفتاحه (unkeyed) ينعكس ويُخزّن في الكاش.
- **التحقق:** جرّب هيدرات مثل `X-Forwarded-Host`/`X-Forwarded-Scheme` وراقب انعكاسها
  مع رؤوس كاش (`X-Cache: HIT`). لـDeception: `/account.php/nonexistent.css`.
- **الأثر:** توزيع محتوى خبيث/سرقة بيانات عبر الكاش.

### 19. Host Header Injection
- **العقلية:** التطبيق يبني روابط/منطق من `Host`.
- **التحقق:** بدّل `Host`/`X-Forwarded-Host` وراقب انعكاسه في روابط، إعادة تعيين
  كلمة المرور، أو منطق التوجيه.
- **الأثر:** تسميم روابط، SSRF، تجاوز.

### 20. HTTP Parameter Pollution (HPP)
- **العقلية:** تكرار البارامتر يُفسَّر بشكل مختلف بين الطبقات.
- **التحقق:** `?role=user&role=admin`؛ راقب أي قيمة تُعتمد (الأولى/الأخيرة/مدموجة).
- **الأثر:** تجاوز فلاتر/تحكم وصول.

### 21. Open Redirect Chaining
- **إشارة الأداة:** علم `REDIRECT_REVIEW`/`LOCATION_REFLECTION`.
- **العقلية:** التوجيه المفتوح وحده منخفض، لكنه يُضخّم OAuth/SSRF/التصيّد.
- **التحقق:** أكّد أن Location خارجي يُحترم فعلًا، ثم اربطه مع تدفّق OAuth (سرقة code)
  أو SSRF.
- **الأثر:** استيلاء حساب، تصيّد موثوق.

---

## خامسًا: خاص بالـAPI

### 22. GraphQL Abuse
- **إشارة الأداة:** tag `graphql`.
- **العقلية:** introspection مفتوح، BOLA على العقد، batching/aliasing لتجاوز
  rate-limit، تسريب حقول.
- **التحقق:** اطلب introspection (إن سمح النطاق) لبناء الخريطة؛ اختبر aliasing
  لطلب متعدد، وBOLA على معرّفات عقد تملكها.
- **الأثر:** تسريب بيانات، تجاوز حدود، BOLA.

### 23. Shadow / Versioned API Drift
- **العقلية:** `v2` مؤمّن لكن `v1` القديم بلا نفس الضوابط.
- **التحقق:** بدّل `/api/v2/` بـ`/v1/`, `/internal/`, `/beta/`, `.json`, أو مسارات
  من ملفات JS قديمة/Wayback. قارن التفويض.
- **الأثر:** الوصول عبر نقاط أضعف.

### 24. Rate-Limit / Anti-Automation Gaps
- **العقلية:** الأفعال الحسّاسة (OTP, coupon, login) بلا حد فعّال.
- **التحقق:** لاحظ غياب rate-limit/CAPTCHA على مسارات حسّاسة عبر عيّنة صغيرة
  محترمة؛ **لا تُنفّذ brute حقيقيًا**.
- **الأثر:** تخمين OTP، حشو بيانات اعتماد.

### 25. Method Override / Content-Type Confusion
- **العقلية:** `X-HTTP-Method-Override: DELETE` أو تبديل نوع المحتوى يتجاوز الضوابط.
- **التحقق:** أرسل GET مع `X-HTTP-Method-Override`, بدّل `Content-Type`
  (`json`↔`form`↔`xml`) وراقب فرق السلوك/التفويض.
- **الأثر:** تجاوز CSRF/تحكم وصول.

---

## سادسًا: البنية التحتية والمنطق

### 26. Subdomain Takeover
- **العقلية:** سجل DNS يشير لخدمة غير مُطالَب بها (S3/Heroku/GitHub Pages...).
- **التحقق:** عدّد الـsubdomains، افحص CNAME المعلّقة ورسائل "NoSuchBucket/404"
  المميزة؛ أكّد الملكية بالمطالبة إن سمح النطاق.
- **الأثر:** سيطرة على نطاق فرعي (تصيّد/سرقة كوكيز).

### 27. Exposed Secrets & Source Leaks
- **إشارة الأداة:** استخراج endpoints من JS (المرحلة 2).
- **العقلية:** أسرار في ملفات JS، source maps (`.js.map`)، `.git/`, `.env`,
  `/actuator`, نسخ احتياطية (`.bak/.old/~`).
- **التحقق:** فتّش ملفات JS عن مفاتيح API/توكنات، اطلب `.map` لإعادة بناء المصدر،
  افحص `/.git/config`. تحقّق من صلاحية أي مفتاح بأقل أثر ممكن.
- **الأثر:** تسريب أسرار، كشف منطق داخلي.

### 28. CORS Misconfiguration Chains
- **إشارة الأداة:** علم `CORS_REVIEW`.
- **العقلية:** `ACAO` يعكس Origin + `credentials:true` = قراءة بيانات موثّقة عبر
  المواقع.
- **التحقق:** من Origin تتحكم به، اقرأ نقطة تُعيد بيانات حساسة بحساب اختبار؛ أكّد أن
  البيانات فعلًا مقروءة (لا مجرد رؤوس).
- **الأثر:** تسريب بيانات المستخدم.

### 29. Race Conditions (TOCTOU)
- **العقلية:** فحص ثم تنفيذ بينهما فجوة زمنية (رصيد، كوبون، حد استخدام).
- **التحقق:** أرسل عددًا صغيرًا من الطلبات المتزامنة على مورد تملكه (single-packet /
  parallel) وراقب هل يُستهلك الكوبون/الرصيد أكثر من مرة. حافظ على عدد منخفض.
- **الأثر:** صرف مزدوج، تجاوز حدود.

### 30. Business Logic Abuse
- **العقلية:** لا "حقن"، بل استغلال قواعد العمل نفسها.
- **التحقق:** قيم سالبة/عشرية للكمية والسعر، تلاعب بالعملة، تكديس الكوبونات، تعديل
  السعر client-side ثم إتمام الطلب، استرداد أكثر من المدفوع — كله على طلبات/حساب
  تملكه.
- **الأثر:** خسارة مالية، تجاوز قيود العمل.

---

## قواعد الاشتباك (Rules of Engagement)
- ✅ اختبر فقط ضمن النطاق المكتوب في البرنامج.
- ✅ استخدم حسابات/موارد تملكها لإثبات التفويض المكسور.
- ✅ قلّل الأثر: أقل عدد طلبات يثبت الثغرة.
- ⛔ لا DoS، لا تدمير بيانات، لا وصول لبيانات مستخدمين حقيقيين.
- ⛔ لا تخزين/نشر بيانات حسّاسة تحصل عليها عرضًا — أبلغ فورًا.
- 📝 وثّق كل خطوة (curl/HAR/لقطات) لتقرير قابل لإعادة الإنتاج.

## ربط سريع: علم الأداة ← التقنية
| علم / وسم في التقرير | التقنيات ذات الصلة |
|---|---|
| `JSON_API`, `identity`, `parameters` | 1 (IDOR), 3 (Mass Assignment), 22 (GraphQL) |
| `STATE_METHODS_ADVERTISED`, `privileged` | 2 (BFLA), 25 (Method Override) |
| `CORS_REVIEW` | 28 (CORS chains) |
| `REFLECTION` | 11 (XSS), 13 (SSTI) |
| `REDIRECT_REVIEW`, `LOCATION_REFLECTION` | 21 (Open Redirect), 7 (OAuth) |
| `AUTHENTICATION_REQUIRED`, `FORBIDDEN` | 5 (403 bypass), 23 (version drift) |
| `WEAK_COOKIE_FLAGS` | 9 (Session), 10 (MFA) |
| `MISSING_SECURITY_HEADERS` | 18 (Cache), 19 (Host header) |
| `auth`, `callback`, `file` params | 6 (JWT), 8 (Reset), 12 (SSRF) |
| JS endpoints (Phase 2) | 27 (Secrets/Source leaks), 23 (Shadow API) |
